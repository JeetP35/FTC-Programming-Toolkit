/* For the ones who haven't ever programmed in Java, 
   just know that there are extra brackets in this template 
   but it is just there to make the code look cleaner 
   however, in your actually code do remove the extra brackets 
   otherwise there will be millioins of errors in your code */
   
   // e.g. : 
   //       {/* name that was declared for the motor */} remove these {} 
   //       and replace the comment with declared name of the motor.


   //       [/* type of input(boolean, double, int, etc) */] remove these {} and replace the comment with valid data type.


   //       [/* gamepad control for the input */] remove these {} and replace the comment with valid variable.


   //       [/* target position of the motor */] remove these {} and replace the comment with valid input.


   //       [ {/* Declared Name of the Motor */}.getCurrentPostion() + /* increment value */] 
   //       remove these [] and replace the comment with valid input (increment vale)
   //       and also remove these {} and replace comment with the deeclared name of the motor.


   

// Packages and imports
package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.HardwareMap;
import com.qualcomm.robotcore.robot.Robot;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.util.Range;
import com.qualcomm.robotcore.hardware.Servo;
import com.qualcomm.robotcore.hardware.CRServo;


@TeleOp
public class /* name of the file */ extends LinearOpMode {
    // Declaring the motors and servos
    private DcMotor /* name of the motor */;
    private Servo /* name of the servo */;
    private CRServo /* name of the continuous rotation servo */;

    @Override
    public void runOpMode(){
        // Initializing the motors and servos
        {/* name that was declared for the motor */} = hardwareMap.get(DcMotor.class, "/* **(The configured Motor name)** */");
        {/* name that was declared for servo */} = hardwareMap.get(Servo.class, "/* **(The configured Servo name)** */");
        {/* name that was declared for continuous rotation servo */} = hardwareMap.get(CRServo.class, "/* **(The configured Continuous Rotation Servo name)** */");

        // Motor Encoder (Usually used for the arm or the extension to have the initial position as 0 and make it run to target position)
        {/* Declared Name of the Motor */}.setTargetPosition(0);
        {/* Declared Name of the Motor */}.setMode(DcMotor.RunMode.STOP_AND_RESET_ENCODER);
        {/* Declared Name of the Motor */}.setMode(DcMotor.RunMode.RUN_TO_POSITION);
        {/* Declared Name of the Motor */}.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);

        waitForStart(); // Wait for the start button to be pressed on the Driver Hub
        while (opModeIsActive()){ // When start button is pressed opModISActive becomes true and the code inside the while loop runs

            // **Variables for input from the controller:
            [/* type of input(boolean, double, int, etc) */] {/* name of the motor */}  = [/* gamepad control for the input */];
            [/* type of input(boolean, double, int, etc) */] {/* name of the servo */}  = [/* gamepad control for the input */];
            [/* type of input(boolean, double, int, etc) */] {/* name of the continuous rotation servo */}  = [/* gamepad control for the input */];



            // Conditional statement to controll the movement of the robot.

            // **Motor Control Drive/Intake Example:
            if ( /* gamepad control is (pressed/moved) or a condition with the input of the gamepad */ ) {
                {/* name of the motor */}.setPower( {/* power of the motor */} );
            } else if ( /* gamepad control is (pressed/moved) in the opposite direction (input is different) */ ) {
                {/* name of the motor */}.setPower( {/* power of the motor */} );

            }else {
                {/* name of the motor */}.setPower( {/* stop power */} );
            }


            // **Motor Control Arm/Extension Example:
            if (/* gamepad control is (pressed/moved) or a condition with the input of the gamepad */) {
                {/* name of the motor */}.setPower( {/* power of the motor */} );
                {/* name of the motor */}.setTargetPosition( [/* target position of the motor */] or [/* current position of the motor */ + /* increment value */] );
            } else if (/*gamepad control is (pressed/moved) in the opposite direction (input is different)*/) {
                {/* name of the motor */}.setPower( {/*power of the motor*/} );
                {/* name of the motor */}.setTargetPosition( [/* target position of the motor */] or [/* current position of the motor */ - /* decrement value */] );
            }else {
                {/* name of the motor */}.setPower( {/* power of the motor */} or {/*stop power */} );
                {/* name of the motor */}.setTargetPosition( [/* set target position of the motor as the current position */] or [/* set target position as the custom position */] );
            }

            /* Note : 
                        Motor does requires input for power and endcoders and input for position aren't necessary,
                        target position is the best for precise and accurate movement like for arm or lift extensions
                        because giving power only [either positive or negative] will only continuously 
                        move the motor in one direction, which is mostly helpful in drive and intake. */



            // **Servo Control Example:
            if (/* gamepad control is (pressed/moved) */) {
                {/* name of the servo */}.setPosition( [/* position of the servo */] );
            } else if (/*gamepad control is (pressed/moved) in the opposite direction (input is different) */) {
                {/* name of the servo */}.setPosition( [/* position of the servo */] );
            }

            /* Note : 
                        Normal Servo only requires input for position only,
                        it won't do anything if you do have an input for power. */



            // **Continuous Rotation Servo Control Example:
            if (/* gamepad control is (pressed/moved) */) {
                {/*name of the continuous rotation servo */}.setPower( {/* power of the Continuous Rotation Servo */} );
            } else if (/* gamepad control is (pressed/moved) in the opposite direction (input is different) */) {
                {/* name of the continuous rotation servo */}.setPower( {/* power of the Continuous Rotation Servo */} );
            }

            /* Note : 
                        Continuous Rotation Servo only requires input for power only,
                        it won't do anything if you do have an input for position. */


            // Telemetry for debugging
            telemetry.addData("/* {Motor name to display on the driver hub} */",  [/* Declared name of the Motor */]  );
            telemetry.addData("/* {Servo name to display on the driver hub} */",  [/* Declared name of the Servo */]  );
            telemetry.addData("/* {Continuous Rotation Servo name to display on the driver hub} */",  [/* Declared name of the Continuous Rotation Servo */]  );
            telemetry.update(); // Display updated telemetry data
        }
    }
}
